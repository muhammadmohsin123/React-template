import { AppBar, Toolbar, Typography, Box } from '@mui/material';

function Header() {
  return (
    <Box
      sx={{
        background: 'blue',
        width: '1300px',
        height: '80px',
      }}
    >
      Header
    </Box>
  );
}

export default Header;
